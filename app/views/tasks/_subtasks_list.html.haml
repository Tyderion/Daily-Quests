=# will_paginate @tasks
%input#search_tasks{value: params[:search]}
%ul
  Available Tasks
  -#Todo: Make this list searchable... or something like that :)
  - for task in @tasks
    = render partial: 'subtask_input', locals: { task: task }

